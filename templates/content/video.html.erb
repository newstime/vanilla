<video-feature
  <%= "video-control data-content-item-id=#{id}" if composing? %>
  style="<%= "top: #{top}px; left: #{left}px; position: absolute;" %>"
  src="<%= video_url %>" thumbnail="<%= video_thumbnail %>"></video-feature>
